import { redirect } from "next/navigation";

/** Hidden until the case study is complete — re-enable by restoring the page content. */
export default function UkFrozenFoodCaseStudyPage() {
  redirect("/case-studies");
}
